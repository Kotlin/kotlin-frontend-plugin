buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

group = 'org.jetbrains.kotlin'

allprojects {
    repositories {
        jcenter()
    }
}
